server:
    port: 2010
management:
    endpoints:
        web:
            exposure:
                include: "*"
spring:
    application:
        name: spring-cloud-server
    profiles:
        active: native, dev
    cloud:
        config:
            server:
                native:
                    searchLocations: classpath:/config