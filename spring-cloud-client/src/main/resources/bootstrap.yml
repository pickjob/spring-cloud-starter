spring:
    application:
        name: spring-cloud-client
    profiles:
        active: dev
    cloud:
        config:
            uri: http://localhost:2010
            fail-fast: true
            retry:
                max-attempts: 5