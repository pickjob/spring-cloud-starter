spring:
    application:
        name: spring-cloud-gateway
    profiles:
        active: dev
    cloud:
        zookeeper:
            connect-string: wsl2:2181
            config:
                enabled: true
                root: spring-cloud-config
                profileSeparator: ":"